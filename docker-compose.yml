services:
  react-app:
    container_name: productivity
    build: ./client/
    ports:
      - "8082:80"
    restart: unless-stopped
    labels:
      tsdproxy.enable: true
      tsdproxy.ephemeral: false
